import { View, Text } from "react-native";
import React from "react";
import { gStyle } from "../../constants";

const MarketPlace = () => {
  return (
    <View style={gStyle.containerWhite}>
      <Text>MarketPlace</Text>
    </View>
  );
};

export default MarketPlace;
